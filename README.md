# Запуск для task 3: 
- в терминале выполните сборку:
  
   `g++ main.cpp bmplib.cpp -o filter.exe`
- запустите программу, передав 3 аргумента:
  
  `filter.exe <исходный_файл> <новый_файл> <тип_маски>`

  пример:

  `filter.exe input.bmp result_8.bmp 8`
